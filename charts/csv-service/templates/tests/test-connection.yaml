apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "csv-service.fullname" . }}-test-connection"
  labels:
    {{- include "csv-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "csv-service.fullname" . }}:8080/health']
  restartPolicy: Never
